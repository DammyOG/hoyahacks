import type { Metadata } from "next";
import Sidebar from "../components/ui/sidebar";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Amplify } from "aws-amplify";
import awsConfig from "@/lib/aws-exports";
import TopNavbar from "@/components/topNavbar";
import { SessionProvider } from "@/components/providers/session";
import { Toaster } from "@/components/ui/toaster";

// Configure Amplify once at the root level
console.log("🔄 Configuring Amplify...");
Amplify.configure(awsConfig);
console.log("✅ Amplify configured");

const geistSans = Geist({
    variable: "--font-geist-sans",
    subsets: ["latin"],
});

const geistMono = Geist_Mono({
    variable: "--font-geist-mono",
    subsets: ["latin"],
});

export const metadata: Metadata = {
    title: "Funnels",
    description: "Generated by create next app",
};

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <html lang="en">
            <body
                className={`${geistSans.variable} ${geistMono.variable} antialiased flex max-w-full min-h-screen`}
            >
                <SessionProvider>
                    <TopNavbar />
                    <section className="mt-16 w-full">{children}
                        <Toaster />
                    </section>
                </SessionProvider>
            </body>
        </html>
    );
}
